O programa lê os nomes e as notas de 3 alunos, calcula a média de cada um e informa se foram aprovados ou reprovados com base nessa média. Para isso, ele utiliza um vetor para armazenar os nomes e uma matriz para armazenar 5 notas por aluno.

Na primeira parte, o programa solicita ao usuário que informe o nome de cada aluno e, em seguida, as 5 notas correspondentes a esse aluno. Depois, exibe uma tabela com as notas de cada um, calcula a média e imprime essa média na tela.

Ao final, com base na média, o programa exibe se o aluno foi aprovado (se a média for maior ou igual a 69) ou reprovado (se for menor que 60).
